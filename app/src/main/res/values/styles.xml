<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NoTitle" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimaryDark">@color/colorLoginPrimaryDark</item>
    </style>

    <style name="MainTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimaryDark">@color/colorMain</item>
    </style>


    <style name="LoginStyle">
        <item name="colorControlNormal">@color/colorLogin</item>
        <item name="android:textColorHighlight">#8f8f8f</item>
        <item name="colorControlActivated">@color/colorLogin</item>
    </style>

    <style name="HintTextStyle" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">10sp</item>
    </style>

    <style name="HintError" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">10sp</item>
        <item name="android:textColor">@color/colorError</item>
    </style>

    <style name="SignupStyle">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/colornotselectcolor</item>
        <item name="colorAccent">@color/colorLogin</item>
        <item name="android:textSize">13sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorControlNormal">@color/colorLogin</item>
        <item name="android:textColorHighlight">@color/colorLogin</item>
        <item name="android:textColor">@color/colorLogin</item>
        <item name="colorControlActivated">@color/colorLogin</item>
    </style>
</resources>
